import React from "react";
import History from "history"

interface Props {
    history: History
}

export default class Extract extends React.Component<Props, object>{
    render() {
        return (
            <div></div>
        );
    }
}